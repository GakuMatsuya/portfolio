<div class="container">
  <div class="row">
    <div class="mt-3 bg-light"><h4>管理者用ログイン</h4></div>
  </div>

  <%= form_with model: @admin, url: admin_session_path, local: true  do |f| %>
    <div class="row mt-4">
      <div class="col-2"><%= f.label "メールアドレス" %></div>
      <div class="col-10"><%= f.email_field :email, autofocus: true, autocomplete: "email" %></div>
    </div>

    <div class="row mt-2">
      <div class="col-2"><%= f.label "パスワード" %></div>
      <div class="col-10"><%= f.password_field :password, autocomplete: "current-password" %></div>
    </div>

  <div class="row mt-3">
    <div class="btn btn-sm btn-wrape btn-primary offset-2">
      <%= f.submit "ログイン", :class=>"btn text-white" %>
    </div>
  </div>
   <% end %>
</div>