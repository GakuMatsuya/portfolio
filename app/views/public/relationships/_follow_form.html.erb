<% if user_signed_in? && user != current_user %>
  <div id="follow_form">
    <% if current_user.following?(user) %>
      <%= render "public/relationships/unfollow_button", user: user %>
    <% else %>
      <%= render "public/relationships/follow_button", user: user %>
    <% end %>
  </div>
<% end %>