<%= form_with model: @user, url: relationships_path, method: :post, remote: true do |f| %>
  <%= f.hidden_field :following_id, :value => @user.id %>
    <%= f.submit "フォローする", class: "btn btn-outline-primary" %>
<% end %>