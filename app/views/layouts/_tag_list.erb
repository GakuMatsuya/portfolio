<div class="tags  mt-5">
  <i class="fas fa-tags"></i>
  <% if review.tag_list.present? %>
    <% review.tag_list.each do |tag| %>
      #<%= tag %>
    <% end %>
  <% else %>
    <p>タグが登録されていません</p>
  <% end %>
</div>